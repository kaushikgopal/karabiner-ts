{
  "name": "karabiner-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate karabiner.json from TypeScript (bun port of karabiner-kt)",
  "scripts": {
    "generate": "bun run src/main.ts",
    "test": "bun test",
    "check": "tsc --noEmit",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  }
}
